<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link type="text/css" href="bootstrap/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">


    <title>Document</title>
</head>
<body>
    

<div class="container">
    <div class="container">
        <h1 style="margin: 10px;" class="text-center" >Editar Cliente</h1>
    </div>
    <a style="margin: 10px;" href="/clientes" class="btn btn-secondary" type="button">regresar</a>
        <form action="/actualizarCliente/<%= clientes[0].idCliente %>" method="post" style="border-width: 1px; border-style: solid; border-radius: 5px; padding: 10px;">

            <div class="form-group">
                <label for="nombre">nombre</label>
                <input id="nombreInput" class="form-control" type="text" name="nombre" value="<%= clientes[0].nombreCliente %>">
            </div>

            <div class="form-group">
                <label for="apellido">apellido</label>
                <input id="apellidoInput" class="form-control" type="text" name="apellido" value="<%= clientes[0].apellidoCliente %>">
            </div>

            <div class="form-group">
                <label for="telefono">Telefono</label>
                <input id="telefonoInput" class="form-control" type="text" name="telefono" value="<%= clientes[0].telefonoCliente %>" onkeypress="return valideKey(event);" maxlength="8">
            </div>

            <div class="form-group">
                <label for="correo">correo</label>
                <input id="correoInput" class="form-control" type="text" name="correo" value="<%= clientes[0].correoCliente %>">
            </div>
            <div class="form-group">
                <label for="municipio" class="form-label">Municipio </label>
                <select class="form-select" aria-label="Default select example" name="municipio">
                    <% for( let municipio = 0; municipio < municipios.length; municipio++ ) { %>
                        <% if (municipios[municipio].nombreMunicipio == clientes[0].nombreMunicipio) { %>
                            <option selected value="<%= municipios[municipio].idMunicipio %>"><%= municipios[municipio].nombreMunicipio %></option>
                        <% } else { %>
                            <option value="<%= municipios[municipio].idMunicipio %>"><%= municipios[municipio].nombreMunicipio %></option>
                         <% } %>
                        
                    <% } %>
                    
                </select>
            </div>

            <button style="margin-top: 10px;" class="btn btn-primary" type="submit">Guardar</button>
        </form>
        
    </div>

</body>
</html>

<script type="text/javascript">
    function valideKey(evt){
        
        // code is the decimal ASCII representation of the pressed key.
        var code = (evt.which) ? evt.which : evt.keyCode;
        
        if(code==8) { // backspace.
          return true;
        } else if(code>=48 && code<=57) { // is a number.
          return true;
        } else{ // other keys.
          return false;
        }
    }
    </script> 